version: "3.8"
services:
  backend:
    build: ./backend
    container_name: backend_c
    ports:
      - "4000:4000"

  # how to add the react part?
  frontend:
    build: .
    container_name: reactapp_c
    ports:
      - "3000:3000"

